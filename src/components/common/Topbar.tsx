import { FC } from "react";

export const Topbar: FC = () => {
  return (
    <div className="fixed top-0 w-full flex justify-between items-center p-5 text-3xl text-white bg-blue-400">
      Quizzer
    </div>
  );
};
